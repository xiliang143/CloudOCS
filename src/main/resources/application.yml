server:
  port: 8080

spring:
  application:
    name: cloud-docs-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clouddocs?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: zyx143322
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10s
      password: zyx143322

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  type-aliases-package: com.cloudocs.**.entity

knife4j:
  enable: true
  setting:
    language: zh_cn

jwt:
  secret: clouddocs-secret-key-for-jwt-token-generation-must-be-at-least-256-bits
  expiration: 86400000
